# W3 — implementation plan (S26)

Spec: `2026-07-10-w3-discussion-function-design.md`. Small commits, `ui` runnable after each;
`npx tsc -b` per commit; i18n overlay edits ride in the SAME commit as their component change
(parity discipline). No DEMO_VERSION bump anywhere in this wave.

## Commit sequence

1. **docs(s26): spec + plan — W3 discussion-as-function** (this commit).
2. **feat(s26): discussion-as-function — pill reframe + stage-badge removal (3.1/3.2 + extras)**
   - `DiscussionPill.tsx`: drop `active` prop/skin; label always `stage.discussionPill`;
     doc comment updated. `DiscussionPill.module.scss`: remove `.active` rules.
   - `InitiativeStageCard.tsx:186`, `FeedEngagePanel.tsx:207`: stop passing `active`.
   - `stageMeta.ts`: delete the `discussion` entry.
   - `StageFeedView.tsx`: remove the `inDiscussion` badge block + `STAGE_META.discussion`
     deref (keep the problem-feed inclusion rule + `panelStage` remap).
   - `HomeView.tsx`: drop the `discussion` SECTIONS entry; fold discussion-stage items into
     `problem` in `realByStage` + the sample reshaping; `ActiveStage` loses `'discussion'`.
   - i18n: retire `stage.discussionPillActive`, `home.discussions`; retire `stage.discussion`
     iff consumer grep comes back empty. fr+sw in this commit.
3. **feat(s26): remove duplicate discussion controls + re-stage samples (3.3–3.5)**
   - `CommunityHome.tsx`: SAMPLE_FEED s2 → `'problem'`.
   - `CommunitySettings.tsx`: render rows from `PIPELINE_STAGES.filter(s => s !== 'discussion')`.
   - `ProblemEngage.tsx`: delete the "Discuss this problem" Button + dead locals/imports.
   - `DiscussionActivityCard.tsx`: delete `onOpen`/`openLabel` + dead `openDiscussion`/navigate.
   - i18n: retire `card.discussProblem`; retire `deliberation.discussion.open` iff orphaned.
4. **feat(s26): write-together sub-views live in the URL (3.6)**
   - `WriteTogetherPage.tsx`: `?draft=new|<id>` search param replaces local view state;
     push on enter, `replace` on start→editor handoff; refresh-on-return effect.
   - `StartDraftForm.tsx`: back arrow → labeled ghost Cancel; `DraftEditor.tsx`: back arrow +
     `onBack` prop retired. Dead `.back` SCSS removed from both modules.
5. **feat(s26): vote-explainer gating + support-CTA vocabulary (3.7/3.8)**
   - `VoteEngage.tsx`: `VoteExplainer` renders only when `!canVote` (above VotePreview).
   - `Currency.tsx` keys `currency.sendTitle`/`sendButton`, `MandateCard.tsx`
     `mandate.card.showSupport`, `AdoptionFramework.tsx` `mandate.adoptCta`: new defaults;
     fr+sw updated in this commit. `MandateEngage` comment updated.
6. **docs(s26): i18n packet — session 26 section** (retired keys annotated, changed strings
   listed for the native reviewers).
7. Closeout docs commit after review + Eston's push gate (§7 flip, §8 changelog, memory,
   S27 prompt).

## Grep gates before each retire/removal

- Key retires: `grep -rn "t('<key>'" src` must be empty post-change; overlay delete in same
  commit; scanner `PARITY OK`.
- Symbol deletions (`openDiscussion`, `onBack`, `.back`, `.active`): grep the SYMBOL across
  src including `.scss` composes before deleting.
- `$gray-400` gate + raw-value scan on any touched SCSS.

## Verification (controller-driven, after commit 5)

`npx tsc -b`; `npm run build`; parity scanner; preview walk 360px light+dark (reload after
scheme flip): stage feed w/ discussion-stage item, community feed (problem + discussion
cards), Home sections, settings rows, write-together round trip (incl. hardware back),
vote card gated + ungated, currency, mandate. fr locale spot-check on touched screens.
Then the adversarial whole-branch review, fixes, and the push gate.
