# Session 6 kickoff — Mandate card/page + CONSUME the commitments/metrics spine

> Paste this into a fresh Claude Code session in the Communities2 repo (branch `ui`) to start Session 6 — the **final** session of the design-consistency + pipeline-redesign roadmap. Sessions 1 (design-system foundation), 2 (problem card + discussion-as-chat + DM), 3 ("Write together" page), 4 (solutions card + commitments/metrics spine), and 5 (vote card + carry the spine) are all shipped & deployed.

---

You're picking up **Session 6** — the **last link of the fixed S4 → S5 → S6 chain**. S4 **authored** the commitments + expert-metrics spine on the solutions; S5 **carried** it onto the ballot; S6 **consumes** it: the Mandate's "What we commit to" / "How we'll know it's working" become **derived from the winning solution** instead of hand-authored fixtures. This session is also the Mandate **card/page redesign**. Start by reading these (don't skip — they carry the locked decisions and the spine you consume):

- Roadmap / master spec: `docs/superpowers/specs/2026-06-25-design-consistency-pipeline-redesign-roadmap.md` (see **§5 Session 6**, **§2 decision #2 — the commitments+metrics spine**, **§6 dependencies**: S4 → S5 → S6 is fixed).
- **S4 spec (the spine shape you read):** `docs/superpowers/specs/2026-06-27-session-4-solutions-card-commitments-spine-design.md` — §4 the `Proposal` shape on `approval.ts` (`commitments`, `expertReviews[].metrics`).
- **S5 spec + plan (the read-back pattern to copy):** `docs/superpowers/specs/2026-06-27-session-5-vote-card-carry-spine-design.md`, `docs/superpowers/plans/2026-06-27-session-5-vote-card-carry-spine.md` — §5 shows exactly how `QVFlow` resolves the proposals contract (2nd `useFlowContract` on `proposalsContractId`) and joins the spine by proposal id. **You reuse this pattern to find the winning solution's spine.**
- S1–S3 specs/plans for the primitives you reuse (`docs/superpowers/specs|plans/2026-06-25..26-*`).
- `DESIGN_SYSTEM.md` and `ARCHITECTURE.md`.

**This is creative UI + a data-derivation decision → begin with the `superpowers:brainstorming` skill.** Resolve the open questions below *with Eston* (one at a time, recommend-then-confirm; offer the visual companion the first time a layout question is genuinely clearer shown than told — the redesigned mandate card at 360px and the two-card preview→detail hierarchy are the candidates). Write a short spec, then `superpowers:writing-plans`, then execute with `superpowers:subagent-driven-development`. Do NOT start coding before Eston approves the design.

## What S1–S5 gave you (reuse it — don't reinvent)
- **`UserIdentity`** (`src/components/shared/UserIdentity.tsx`) — `[flag] Name [verified-shield]`. (Decision held in S4/S5: per-author bylines show flag+name only, NO trustState/shield — consistent across SolutionsBoard + QVFlow. Keep that consistency if the mandate shows an author byline.)
- **The scope badge:** `scope: 'global'|'community'` on `problems.ts` → `StagePost.scope` → "Global problem"/"Community problem" `Badge` in `InitiativeStageCard`. The MandateActivityCard's `fullPost` currently does NOT carry `scope` (only sdg/countryCount/source) — add it if the mandate card should show it (mirror the S5 VoteActivityCard one-liner).
- **`src/utils/regions.ts`** (NEW in S5) — `REGIONS`, `regionOf(countryCode)`, `regionColorVar(id)`, `RegionId` + `$region-*`/`--region-*` tokens (6 regions + `other`, AA light+dark). Reuse if the mandate shows a regional breakdown; otherwise the mandate uses `CountryPresence` (flags) for its jurisdiction list.
- **THE SPINE (the load-bearing reuse):** the winning solution's `commitments: string[]` + `expertReviews[].metrics` live on the **`Proposal` in `src/services/demo/demoContracts/approval.ts`**, read via `approvalApi.getProposals(...)`. **S5's `QVFlow` already reads them back** (2nd `useFlowContract(`${parentContractId}_proposals`, …, 'proposalsContractId')` + join by proposal id `p0..p3`). **S6 reads the SAME spine for the winning solution** — copy that pattern.
- **The winner:** `qv.getResults` (`qvApi`) returns sqrt-vote totals per proposal id; the winning solution = the max. Join its id to the `approval` proposal → its commitments + metrics.
- The kit (`Button`/`Card`/`Modal`/`Badge`/`SegmentedControl`/`CountryPresence`) + canonical `$stage-*` palette. Tokens only; verify light + dark.
- **Seam discipline:** all reads via `src/services/api.ts`; demo seam emits **no `contract_write` events** → re-fetch after writes (S6 is mostly reads; conviction staking already follows this).

## Session 6 goal (roadmap §5 Session 6)
Redesign the Mandate card/page and make its commitments/metrics **derived from the winning solution** (the payoff of decision #2):

1. **Remove the blue "What we decided" box** (`MandateCard.tsx` `.decided`) → plain text below the title: **the problem first, then the winning solution**.
2. **Move Share next to "Read the full mandate"** (currently Share sits below — put them side by side).
3. **Swap the two-card hierarchy:** the redesigned **top card becomes the preview** on the global Mandate page (bottom-nav); the currently-similar second card becomes the **click-through detail**.
4. **Label the country counts:** `mandate.countries` (5) = jurisdiction; `provenance.countries` (18) = deliberation reach — make the distinction legible (today both render as bare "{n} countries").
5. **Consume the spine:** render the winning solution's **commitments** as the mandate's "What we commit to" (articles) and the **expert metrics** as "How we'll know it's working" (indicators). `fixtures/mandate.ts` `articles`/`indicators` become **derived** rather than hand-authored.
6. **Review gate:** `/code-review` (the local multi-model panel).

## Open design questions to resolve in the brainstorm (with my leanings)
- **★ How the mandate derives the winning solution's commitments/metrics (the load-bearing consume).** The mandate page is keyed `/mandate/:communityId/:initiativeId`; resolve the initiative's **vote** contract (`voteContractId`) for the winner and its **proposals** contract (`proposalsContractId`) for the spine — exactly the S5 read-back pattern. Winner = top `qv.getResults` id → `approvalApi.getProposals[id]` → `commitments`/`expertReviews[].metrics`. **Lean:** derive `articles` from `commitments` (each commitment → one article body) and `indicators` from the flattened `expertReviews[].metrics` (each metric → one indicator), with a **graceful fallback to the hand-authored `MANDATES_BY_KEY` fixture** when an initiative has no spine. The rich fixture parts that AREN'T in the spine (preamble, provenance, adopters, jurisdiction countries) stay fixtures. Confirm: derived-with-fallback vs fully-derived; and where the derivation lives (a `useMandate`/selector layer over `MANDATES_BY_KEY`, NOT in components).
- **The article/indicator mapping (shape mismatch).** `MandateArticle` = `{id,title,body}`, `MandateIndicator` = `{label,target}` — but the spine's commitments + metrics are **plain strings**. **Lean:** article = `{id:'art-N', title: '' (or omit), body: commitment}`; indicator = `{label: metric, target: '' }` (and adjust `MandateDocument`/`MandateCard` to render gracefully when `title`/`target` are empty). Confirm the exact shape so `buildSpec` (the machine-readable JSON) and the plain view both stay coherent.
- **Which demo initiative showcases the derived mandate + the seed.** Today only `adaptation` (the `stage:'mandate'` showcase) has a `MANDATES_BY_KEY` entry, and it has **no commitments/metrics seed** on its winning solution. **Lean:** seed `adaptation`'s winning solution with commitments + expert metrics (the S5 pattern in `deliberation.ts` `PROPOSAL_COMMITMENTS_BY_KEY`/`PROPOSAL_EXPERT_REVIEWS_BY_KEY`) so the mandate page renders DERIVED articles/indicators that match its `provenance.voteWinner`; keep the hand-authored adaptation fixture as the fallback for the parts that don't derive. Bump `DEMO_VERSION` (`global-v9` → `v10`). Confirm whether to also wire `privacy` (S5's vote-stage showcase) for when it advances to mandate, or leave that to runtime.
- **Remove the blue box → problem-then-solution plain text.** **Lean:** replace `MandateCard`'s `.decided` block with plain text: the **problem statement** (from the initiative framing / `ProblemFraming`) then the **winning solution** text (`provenance.voteWinner`, or the derived winner). Confirm the source of the problem line (initiative `statement`/framing vs `mandate.subtitle`).
- **Share next to Read.** **Lean:** one actions row — `Read the full mandate` (primary) + `Share` (secondary) side by side, wrapping gracefully at 360px. Confirm.
- **Swap the two-card hierarchy.** Today `MandatePage` stacks `MandateCard` (hero) above `MandateDocument` (full doc) on the same page. **Lean:** the bottom-nav/global Mandate view shows the redesigned **card as a preview**; "Read the full mandate" routes/scrolls to the **document as the click-through detail**. Confirm what "global Mandate page (bottom-nav)" maps to (the `/stage/mandate` feed vs `/mandate/...`) and the exact preview→detail navigation.
- **Label the 5-vs-18 country counts.** **Lean:** label them explicitly — e.g. "5 countries · jurisdiction" (the `CountryPresence` over `mandate.countries`) vs "18 countries · deliberated" (`provenance.countries`). Confirm copy (and keep it short for 360px).
- **Layout at 360px.** The redesigned mandate card (no blue box, problem→solution, labelled counts, Read+Share row) + the derived commitments/metrics in the document. Resolve in the brainstorm (visual companion for the card + the preview→detail hierarchy).

## Current-state pointers (read during brainstorm)
- **The mandate card (redesign target):** `src/components/mandate/MandateCard.tsx` (+ `.module.scss`) — the `.decided` "What we decided" box (lines ~73–76), the three signals (Reach uses `provenance.countries`=18 + `CountryPresence` over `mandate.countries`=5), `Read the full mandate` (full-width primary) + `Share` (secondary below). `MANDATE_DOC_ANCHOR_ID` scrolls to the document.
- **The published document (consumes articles/indicators):** `src/components/mandate/MandateDocument.tsx` — `articles` → "What we commit to", `indicators` → "How we'll know it's working", plus `buildSpec()` (machine-readable JSON) and the plain/spec `SegmentedControl`. This is where the **derived** spine renders.
- **The page composition:** `src/components/mandate/MandatePage.tsx` (+ `MandatePage.demo.ts`) — stacks card + document; the global Mandate route. `JourneyRecap` + `useMandateJourney` feed the arc recap.
- **The card hosts (stage feed / community page):** `src/components/community/MandateActivityCard.tsx` (builds the `StagePost` — add `scope` here if wanted; mandate is the LAST stage so NO advance bar) → `src/components/initiative/stages/MandateEngage.tsx` → `src/components/stages/MandateStage.tsx` (conviction staking via `ConvictionStaking`, gated by `StageGate`).
- **The fixtures to make derived:** `src/services/demo/fixtures/mandate.ts` — `PublishedMandate` (`articles: MandateArticle[]`, `indicators: MandateIndicator[]`, `provenance.voteWinner`, `countries` vs `provenance.countries`), `MANDATES_BY_KEY` (only `adaptation`), `DEFAULT_MANDATE_KEY = 'adaptation'` (every other initiative falls back to adaptation today).
- **The spine source + read-back:** `src/services/demo/demoContracts/approval.ts` (the `Proposal` spine) + `src/components/collaboration/flows/voting/approvalApi.ts` (`getProposals`) + `qvApi.ts` (`getResults` for the winner). **Copy QVFlow's resolve-and-join pattern** (`src/components/collaboration/flows/voting/QVFlow.tsx`, the 2nd `useFlowContract` + id-join).
- **The seeder:** `src/services/demo/seedDemoCommunity.ts` — `PROPOSAL_COMMITMENTS_BY_KEY` / `PROPOSAL_EXPERT_REVIEWS_BY_KEY` in `fixtures/deliberation.ts` (add an `adaptation` entry, S5 pattern); the qv winner is whichever proposal the seeded `qvAllocationPattern` ranks top.

## S5 hand-off — relevant to S6
- **The spine read-back pattern is proven** — S5's `QVFlow` resolves the proposals contract via a 2nd `useFlowContract` on `proposalsContractId` (parent = initiative) and joins by proposal id (`p0..p3` match across qv/approval). Reuse it verbatim for the winning solution.
- **`adaptation` (the `stage:'mandate'` showcase) has NO commitments/metrics seed yet** — S4 seeded amr/jobs, S5 seeded privacy. Seed adaptation as part of S6 so the mandate renders derived data (and make the seed match `provenance.voteWinner` so the card's plain text and the derived articles agree).
- **KEY seam learning (do not relitigate):** ui contract method/field names MUST match Ouri's real contract exactly — keep `addProposal`/`proposal_id`/`get_results` etc. "Solution"/"mandate" are presentation vocab only.
- **MandateEngage keeps conviction staking** despite the roadmap calling mandate "read-only" — flagged spec discrepancy, KEEP it (it's the only commitment control). Don't remove it.
- **Verified-shield decision (S4/S5):** per-author bylines = flag+name only (no shield), consistent across boards. Hold that unless Eston revisits it platform-wide.
- Still-deferred (not S6's job unless you're in the file): the dormant `PositionsBoard`/`AnchoredThread`/`ParticipationMeter`/`CoPresenceBar` + `PRESENCE_*` fixtures; the 3 S2 cleanups; the for-Ouri Write-Together-registry-as-contract note; S4 spine method docs + S5 client-side lock/turnout notes.

## Workflow + constraints (same discipline as S1–S5)
- **Branch `ui`, keep it runnable.** Stay behind `src/services/api.ts`; never call a real server from a component. Demo seam emits no `contract_write` events → re-fetch after writes.
- **Derivation lives in a selector/hook layer, not components** — a `useMandate(initiativeId)` (or similar) that reads `MANDATES_BY_KEY` + the winning solution's spine and returns a `PublishedMandate` with derived `articles`/`indicators` (fallback to fixture). Document any new read path for Ouri with `// FOR OURI` (S6 should need NO new contract methods — it reads the existing spine + qv results).
- **Tokens only**; reuse `UserIdentity` + `CountryPresence` + the kit; **360px flagship, verify light + dark.** AA gates per `DESIGN_SYSTEM.md` (no `$gray-400` body text; ≥44px touch; focus rings).
- **New user-facing strings ship at fr + sw key parity** (`src/i18n/fr.ts` + `sw.ts`; en inline via `t('key','English')`; the dicts are **flat dotted keys**, `en.ts` carries no entry for these — English is the inline default). After adding, run the key-parity check (`diff <(grep -oE "'[a-zA-Z0-9_.]+':" fr.ts|sort -u) <(… sw.ts …)` → empty) + a code-ref↔i18n cross-check. Append new strings to `docs/i18n-native-review-candidates.md`.
- **Production build runs `tsc -b`** — `npm run build` clean before each commit. No test framework: verify via build + the `preview_*` tools (dev server `gloki-dev`, port 5173) at 360px. The `adaptation` initiative ("A Universal Climate Adaptation Fund", on the Climate Resilience Assembly community) is the seeded `stage:'mandate'` showcase to verify against. (Note: `StageGate` gates conviction staking for unverified users — pre-existing; the published-mandate card/document render regardless.)
- If new demo data is seeded (adaptation spine), **bump `DEMO_VERSION`** in `src/services/demo/mockApi.ts` (S5 ended at `global-v9`; next is `v10`).
- Execute **subagent-driven** (fresh implementer per task — cheapest tier when the plan carries full code, mid-tier for integration; per-task spec+quality review; an **Opus whole-branch review** at the end). Track a ledger (`.superpowers/sdd/progress.md`); for i18n parity + prune do your own grep cross-check. Tip from S5: order tasks so each builds clean (define optional props/derivation layer before wiring callers).
- **Gate:** run the local multi-model review panel (`/code-review` → `local-review` skill) on the session diff — **do NOT pass `--quit-chrome`** (Eston keeps Chrome open). NOTE from S5: there is **no `GEMINI_API_KEY` in `.env`** (the gemini-flash reviewer won't run unless one is added), and the local Ollama models RAM-skip when Jellyfin is transcoding — if the panel comes back zero-coverage, say so and lean on the per-task + Opus whole-branch reviews (Eston accepted this in S5). Then **push `origin/ui`** to deploy (PR #20's ✗ vs `main` is expected divergence, not a build failure). **Confirm the seed/demo-content + the deploy with Eston before pushing.**
- Update project memory after the session (`project_consistency_pipeline_redesign_jun2026` + the `MEMORY.md` index line). **This completes the 6-session roadmap** — note that in memory.

When ready, kick off with the brainstorming skill and the first open question (★ the consume: derive the mandate's articles/indicators from the winning solution's commitments+metrics — read via the S5 proposals-contract pattern, fallback to the hand-authored fixture) — and remember this is the payoff of the whole S4→S5→S6 chain: the words a community wrote as commitments, and the metrics an expert attached, surface as the binding mandate.
