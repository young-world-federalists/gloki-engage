# Agent instructions

This file intentionally defers to **[CLAUDE.md](./CLAUDE.md)** — the single source of
project instructions for all coding agents (Claude Code, and any tool that reads
`AGENTS.md`).

Read `CLAUDE.md` for the stack, branch model, key patterns, routing, flows, and the
design system (`DESIGN_SYSTEM.md`). Don't duplicate project guidance here — keep one
authoritative copy in `CLAUDE.md`.
